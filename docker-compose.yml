version: '3.8'

services:
  postgres:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: signage
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - '5432:5432'

  backend:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    depends_on:
      - postgres
    environment:
      DATABASE_URL: postgresql://neondb_owner:npg_B8imsfdLxO9Y@ep-soft-dust-a1p6geg5-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
      CLERK_SECRET_KEY: sk_test_Wz4rgtqRoCBEJuN0MoKUazSlfKs9ByEM03kqbhFOII
      CLERK_ISSUER: https://flying-egret-38.clerk.accounts.dev
    ports:
      - '3000:3000'
    volumes:
      - .:/app
      - /app/node_modules

volumes:
  pgdata:
